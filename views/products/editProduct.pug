extends ../layout
block content
    .container(style="width: 70%")
        .row
            form
            .col(style="margin-right: 2rem;")
                .form-group
                    h4 Products and their existing barcodes
                    select.form-control
                        each prod in products
                            option.form-control #{prod.Name} - #{prod.Barcode} 
            .col
                h4 Edit Product  
                .form-group
                    input#barcode.form-control(placeholder='Barcode' name='barcode')    
                .form-group
                    input#price.form-control(placeholder='Price' name='price')
                .form-group
                    input#name.form-control(placeholder='Product Name' name='name')
                .form-group
                    input#brand.form-control(placeholder='Brand' name='brand')    
                .form-group
                    input#category_id.form-control(placeholder='Category' name='category_id')
        .row(style='margin: 2rem 30% 1rem 30%;')
            a.button.btn.btn-dark(style="padding: 1rem; margin-right: 1.5rem; border-radius:15px;" href="/products" type="button") Back        
            button.btn.btn-dark(style=" padding: 1rem; border-radius:15px;" id='submit-btn' type='submit' onClick="sendData()") Change fields provided
            script(src="../js/editProduct.js")